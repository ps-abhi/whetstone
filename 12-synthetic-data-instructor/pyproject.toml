[project]
name = "12-synthetic-data-instructor"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "aiolimiter>=1.2.1",
    "instructor>=1.15.3",
    "openai>=2.43.0",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.17",
]
